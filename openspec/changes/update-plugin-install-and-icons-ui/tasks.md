## 1. Implementation
 - [x] 1.1 阅读 `openspec/project.md` 与 `specs/plugin-system/spec.md`、`specs/desktop-ui/spec.md`，明确现状与约束
- [x] 1.2 修复示例插件 icon 加载路径（清单与渲染层回退逻辑）
 - [x] 1.3 统一安装/刷新/插件目录的图标与文字垂直对齐（Flex 布局与尺寸规范）
- [x] 1.4 简化安装入口：移除“在线安装”和“插件商店”标签页与相关逻辑，仅保留本地文件
- [x] 1.7 添加“添加调试插件”按钮；弹出 dialog 渲染 `pluginDevTools.vue`，添加完毕后保存在插件列表中
 - [x] 1.8 在插件列表中为调试插件添加“调试插件”标注（徽标/标签）
- [x] 1.9 普通插件详情移除“开发工具”标签页；仅在调试插件详情中显示
 - [x] 1.10 插件详情的 dialog 改为全屏模式打开（样式与交互适配）
 - [x] 1.11 移除插件过滤栏目；将搜索栏移动到插件列表卡片 header；其余筛选项丢弃
 - [x] 1.12 去掉插件详情的错误管理分栏
 - [x] 1.13 日志分栏单行布局：类型/信息/时间同一行；信息溢出显示“更多”并弹框展示完整内容
 - [x] 1.14 插件逻辑调整：ui页与新窗口页互斥，但无论选择哪种均可同时拥有 overlay 页
 - [x] 1.15 列表卡片为含 ui/window 的插件显示“查看”按钮：进入 ui 页或创建新窗口加载 window 页
 - [x] 1.16 列表卡片为含 overlay 的插件显示“复制链接”按钮：点击弹出“复制成功”提示
 - [x] 1.17 列表卡片“三点”更多菜单增加“在侧边栏显示”选项：选中后在侧边栏插件管理目录显示并进入 ui/window 页
 - [x] 1.18 侧边栏默认全部展开
 - [x] 1.19 排查并修复 topbar 用户信息弹窗“秒退”问题
 - [x] 1.20 排查并修复 topbar 房间信息点击无反应问题

## 2. Validation
- [x] 2.1 严格遵循工作区规则：不启动渲染进程开发服务器；不编写或运行测试（仅静态走查与 typecheck）
- [x] 2.2 手工确认 UI：列表/安装区域的图标与文本对齐；示例插件图标正常显示；安装入口只保留本地文件
 - [x] 2.3 手工确认“添加调试插件”按钮能打开并正确渲染 `pluginDevTools.vue`
 - [x] 2.4 检查调试插件在列表中显示“调试插件”标注；普通插件详情不显示“开发工具”标签页
 - [x] 2.5 验证插件详情全屏 dialog 的打开/关闭逻辑、滚动与窗口适配
 - [x] 2.6 检查卡片 header 中的搜索功能；移除过滤栏目后无断裂与残留逻辑
 - [x] 2.7 确认插件详情不再显示错误管理分栏
 - [x] 2.8 验证日志分栏单行布局，信息溢出显示“更多”，弹框展示完整内容
 - [x] 2.9 验证 ui/window 互斥逻辑与 overlay 同时存在的兼容性
 - [x] 2.10 验证“查看”按钮行为：ui 页直接进入；window 页创建新窗口加载
 - [x] 2.11 验证“复制链接”按钮复制成功提示；确认链接可用于 OBS 浏览器源
 - [x] 2.12 验证“三点”菜单的“在侧边栏显示”选项：侧边栏出现且点击进入对应页面
 - [x] 2.13 验证侧边栏默认展开行为
 - [x] 2.14 验证 topbar 用户信息弹窗交互稳定，不再“秒退”
 - [x] 2.15 验证 topbar 房间信息点击后正常响应

## 3. Notes
- 遵循 `openspec/AGENTS.md` 的提案/实施流程；变更为 UI 布局与入口精简，未更改主进程 IPC 与持久化逻辑。

## 4. Rectifications
- [x] 4.1 修复插件图标加载失败：为 `base-example` 插件的 `icon.svg` 提供正确静态资源路径或读取方式；在渲染层添加同源/404 回退策略，保证图标正常显示。
- [x] 4.2 修复“复制链接”404：校正 `overlay` 链接构造与清单声明，确保返回的 URL 可访问（默认回退至 `overlay.html` 或对应 SPA 路由），并可在 OBS 浏览器源中使用。
- [x] 4.3 修复“点击查看”404：核验 `resolvePrimaryHostingType` 与 `/plugins/frame/:id` 路由，确保 UI 托管进入框架页、Window 托管创建新窗口；修复 `base-example` 的“查看”行为。
- [x] 4.4 对齐右上角按钮图标：统一 `Topbar.vue` 中按钮的 `t-icon` 尺寸与文本垂直对齐（行高/对齐方式/间距），消除“图标比文字高”的问题。
- [x] 4.5 “在侧边栏显示”改为复选框：将列表卡片“三点”菜单的“在侧边栏显示”调整为 checkbox 项，显示选中状态并支持再次取消。
- [x] 4.6 侧边栏分类位置调整：勾选“在侧边栏显示”后，在“插件管理”一级分类下新增以插件名命名的二级分类，而非底部新建一行；调整分组与排序逻辑。
 - [x] 4.7 修复插件 UI 桥接：为 `base-example` 注入基于 `postMessage` 的 `props.api`（`getConfig`/`setConfig`/`overlay`）；在 `PluginFramePage.vue` 实现 `bridge-request` 处理并派发 `lifecycle` 与 `readonly-store` 事件；静态走查确认无残留。
 - [x] 4.8 过滤配置中的 token 字段并修复 overlay 响应：在 `PluginFramePage.vue` 的 `get-config` 分支不向 UI 传递 `token` 字段；`overlay` 操作返回完整结果对象供 UI 判断；同步更新 `base-example` 的演示逻辑，展示配置时移除敏感字段；去除只读仓库的初始缺失提示。
